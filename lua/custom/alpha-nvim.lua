require('alpha').setup(require('dashboard').config)
-- require('alpha').setup(require('alpha.themes.startify').config)
-- require('alpha').setup(require('theta').config)
-- require('alpha').setup(require('alpha.themes.dashboard').config)
